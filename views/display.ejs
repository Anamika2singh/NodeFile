<!DOCTYPE html>
<html>

    <body>
        <% if(users.length>0) {%>
            
        <table border="1">
            <tr>
                <th>Index</th>
                <th>fullName</th>
                <th>Lastname</th>
                <th>Email</th>
                <th>Mobile</th>
                <th>Arrivalt</th>
                <th>Departtime</th>
                <th>Date</th>
            </tr>
            <% for(var i=0;i<users.length;i++) { %>
                <tr>
                    <td><%=i %></td>
                    <td><%=users[i].fname %></td>
                    <td><%=users[i].lname %></td>
                    <td><%=users[i].email %></td>
                    <td><%=users[i].mobile %></td>
                    <td><%=users[i].arrivaltime %></td>
                    <td><%=users[i].departuret %></td>
                    <td><%=users[i].date %></td>
                    <!-- <td>
                        <a href='/attendance/<%=users[i]._id %>'>viewattendance</a>|
                       
                    </td> -->
                </tr>
                <% } %>
        </table>
        <% }
        else {%>
            no record found
            <% }  %>
    </body>
</html>